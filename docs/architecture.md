# orobot-firmware Architecture

This document previously described the old JavaScript architecture (`scripts/keep-alive.js`).
The codebase has been rewritten in TypeScript.

**For current architecture:**
- See [README.md](../README.md) for an overview and module map
- See [`src/`](../src/) for the implementation
- Key entry points: `src/index.ts` (production), `src/dev.ts` (local dev)
